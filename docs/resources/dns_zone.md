---
page_title: "thalassa_dns_zone Resource - terraform-provider-thalassa"
subcategory: "DNS"
description: |-
  Create and manage a DNS zone in Thalassa Cloud
---

# thalassa_dns_zone (Resource)

Create and manage a DNS zone in Thalassa Cloud

~> **Early access:** DNS is in early access. The DNS feature gate must be enabled for your organisation. The API and Terraform resource schema may change in future provider releases.

See [DNS documentation](https://docs.thalassa.cloud/docs/dns/).

Deleting a zone permanently removes all records. DNS changes propagate in approximately 25–40 seconds.

## Example Usage

```terraform
resource "thalassa_dns_zone" "example" {
  zone_name   = "example.com"
  description = "Example zone"
}
```
<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `zone_name` (String) DNS zone name (e.g. example.com). Renaming requires recreation.

### Optional

- `annotations` (Map of String)
- `description` (String)
- `labels` (Map of String)
- `organisation_id` (String)

### Read-Only

- `created_at` (String)
- `id` (String) Platform identity of the DNS zone (dnsz-…).
- `object_version` (Number)
- `slug` (String)
- `updated_at` (String)

## Import

Import ID: zone platform identity (`dnsz-…`).

```shell
#!/bin/bash
# Example: terraform import thalassa_dns_zone.example dnsz-abc123
terraform import thalassa_dns_zone.example dnsz-abc123
```
